const TaskPilotLogoIcon = ({ className = 'h-5 w-5', alt = 'TaskPilot' }) => (
  <img src="/logo.png" alt={alt} className={`shrink-0 object-contain ${className}`} draggable="false" />
);

export default TaskPilotLogoIcon;